In the method below, the expression writer is not nullable at the point it is used. ```java private void decodeAttribute(int attributeNS, int attrValDataType, int attrValData, String shortNsName, String attrName) { if (attrValDataType == TYPE_REFERENCE) { if (writer == null) { return; } // reference custom processing String resName = resNames.get(attrValData); if (resName != null) { CodeWriter w = writer; w.add('@'); if (resName.startsWith("id/")) { w.add('+'); } w.add(resName); } else { String androidResName = ValuesParser.getAndroidResMap().get(attrValData); if (androidResName != null) { CodeWriter w = writer; w.add("@android:").add(androidResName); } else if (attrValData == 0) { CodeWriter w = writer; if (w == null) { return; } w.add("@null"); } else { CodeWriter w = writer; w.add("0x").add(Integer.toHexString(attrValData)); } } } else { String str = valuesParser.decodeValue(attrValDataType, attrValData); memorizePackageName(attrName, str); if (isDeobfCandidateAttr(shortNsName, attrName)) { str = deobfClassName(str); } if (writer == null) { attachClassNode(null, attrName, str); return; } CodeWriter w = writer; attachClassNode(w, attrName, str); w.add(str != null ? StringUtils.escapeXML(str) : "null"); } } ``` Please rewrite the method so that at the line " writer.add(str != null ? StringUtils.escapeXML(str) : "null");", at the occurrence of: "writer" is replaced with `Nullability.castToNonnull(writer)`. **Important**: If the expression "writer" is referenced further times after the targeted line to rewrite: Everywhere, where non-null is expected for the expression "writer" (e.g., at dereference sites), cast the expression to non-null, to prevent follow-on NullAway errors. However, if you would have to add many casts, prefer casting at definition time or reassigning the expression to a local variable and casting there, if this is possible without breaking the code. **Example** - Target non-nullable expression `foo`: ```java public void test(){ String l = this.foo.getBar(); String l2 = this.foo.getFoo(); // this.foo is used again } ``` will be: ```java public void test(){ String l = Nullability.castToNonnull(this.foo).getBar(); String l2 = Nullability.castToNonnull(this.foo).getFoo(); // also added a cast here } ``` ### Requirements: - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method wrapped in the XML format shown below. Please only provide the fixed code snippet in XML format. I just need the XML response, no other information is needed. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within a ```java ... ``` block. Your response should only be the xml format below, no additional text. ### Response Format: **Provide a meaningful fix:** <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here, JUST THE METHOD. ``` ]]> </code> </response>