In the method below, the expression writer is not nullable at the point it is used. ```java private void parseElement() throws IOException { if (firstElement) { firstElement = false; } else if (writer != null) { Nullability.castToNonnull(writer).incIndent(); } if (is.readInt16() != 0x10) { die("ELEMENT HEADER SIZE is not 0x10"); } // TODO: Check element chunk size is.readInt32(); int elementBegLineNumber = is.readInt32(); int comment = is.readInt32(); int startNS = is.readInt32(); int startNSName = is.readInt32(); // actually is elementName... if (writer != null && !isLastEnd && !"ERROR".equals(currentTag)) { Nullability.castToNonnull(writer).add('>'); } isOneLine = true; isLastEnd = false; currentTag = deobfClassName(getString(startNSName)); currentTag = getValidTagAttributeName(currentTag); if (writer == null) { return; } Nullability.castToNonnull(writer).startLine('<').add(currentTag); Nullability.castToNonnull(writer).attachSourceLine(elementBegLineNumber); int attributeStart = is.readInt16(); if (attributeStart != 0x14) { die("startNS's attributeStart is not 0x14"); } int attributeSize = is.readInt16(); if (attributeSize != 0x14) { die("startNS's attributeSize is not 0x14"); } int attributeCount = is.readInt16(); int idIndex = is.readInt16(); int classIndex = is.readInt16(); int styleIndex = is.readInt16(); if (writer != null && nsMap != null && ("manifest".equals(currentTag) || Nullability.castToNonnull(writer).getIndent() == 0)) { for (Map.Entry<String, String> entry : Nullability.castToNonnull(nsMap).entrySet()) { String nsValue = getValidTagAttributeName(entry.getValue()); Nullability.castToNonnull(writer).add(" xmlns"); if (nsValue != null && !nsValue.trim().isEmpty()) { Nullability.castToNonnull(writer).add(':'); Nullability.castToNonnull(writer).add(nsValue); } Nullability.castToNonnull(writer).add("=\"").add(StringUtils.escapeXML(entry.getKey())).add('"'); } } boolean attrNewLine = attributeCount != 1 && ATTR_NEW_LINE; for (int i = 0; i < attributeCount; i++) { parseAttribute(i, attrNewLine); } } ``` Please rewrite the method so that at the line " writer.add('>');", at the occurrence of: "writer" is replaced with `Nullability.castToNonnull(writer)`. **Important**: If the expression "writer" is referenced further times after the targeted line to rewrite: Everywhere, where non-null is expected for the expression "writer" (e.g., at dereference sites), cast the expression to non-null, to prevent follow-on NullAway errors. However, if you would have to add many casts, prefer casting at definition time or reassigning the expression to a local variable and casting there, if this is possible without breaking the code. **Example** - Target non-nullable expression `foo`: ```java public void test(){ String l = this.foo.getBar(); String l2 = this.foo.getFoo(); // this.foo is used again } ``` will be: ```java public void test(){ String l = Nullability.castToNonnull(this.foo).getBar(); String l2 = Nullability.castToNonnull(this.foo).getFoo(); // also added a cast here } ``` ### Requirements: - Do not import any additional libraries or classes - Do not add Java comments, but also do not remove any preexisting comments. - Only modify relevant lines as required - Do not address any other issues - Return only the modified method wrapped in the XML format shown below. Please only provide the fixed code snippet in XML format. I just need the XML response, no other information is needed. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within a ```java ... ``` block. Your response should only be the xml format below, no additional text. ### Response Format: **Provide a meaningful fix:** <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here, JUST THE METHOD. ``` ]]> </code> </response>