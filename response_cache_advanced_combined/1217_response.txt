<response>
  <success>true</success>
  <code><![CDATA[
  ```java
  @SuppressWarnings("unused")
 	private void parseTypeSpecChunk(long chunkStart) throws IOException {
 		if (is == null) {
 			throw new IOException("ParserStream not initialized");
 		}
 		Nullability.castToNonnull(is).checkInt16(0x0010, "Unexpected type spec header size");
 		int chunkSize = Nullability.castToNonnull(is).readInt32();
 		long expectedEndPos = chunkStart + chunkSize;
 
 		int id = Nullability.castToNonnull(is).readInt8();
 		Nullability.castToNonnull(is).skip(3);
 		int entryCount = Nullability.castToNonnull(is).readInt32();
 		for (int i = 0; i < entryCount; i++) {
 			int entryFlag = Nullability.castToNonnull(is).readInt32();
 		}
 		if (Nullability.castToNonnull(is).getPos() != expectedEndPos) {
 			throw new IOException(String.format("Error reading type spec chunk at offset 0x%x", chunkStart));
 		}
 }
  ```
    ]]>
  </code>
</response>