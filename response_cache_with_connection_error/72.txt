In the method below, the expression Within the body of fillImplementsCache, the first statement is: if (nameMap == null) { throw new JadxRuntimeException("Classpath must be loaded first"); } This is a guard clause. If nameMap is null, the method immediately throws an exception and terminates; no subsequent lines are executed. Therefore, reaching the line Map<String, List<String>> map = new HashMap<>(nameMap.size()); is only possible if nameMap != null at that point in the control flow. Under standard Java semantics (no intervention from tools that can violate normal control flow, such as bytecode instrumentation that skips the null check), this guarantees that nameMap cannot be null when its size() method is called for the HashMap constructor. is not nullable at the point it is used. ```java nameMap ``` Please rewrite the method so that at the line "private void fillImplementsCache() { if (nameMap == null) { throw new JadxRuntimeException("Classpath must be loaded first"); } Map<String, List<String>> map = new HashMap<>(nameMap.size()); List<String> classes = new ArrayList<>(nameMap.keySet()); Collections.sort(classes); for (String cls : classes) { for (String st : getSuperTypes(cls)) { map.computeIfAbsent(st, v -> new ArrayList<>()).add(cls); } } implementsCache = map; }", at the occurrence of: " Map<String, List<String>> map = new HashMap<>(nameMap.size());" is replaced with `Nullability.castToNonnull(nameMap)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>