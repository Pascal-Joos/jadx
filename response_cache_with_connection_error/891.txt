In the method below, the expression Within the body of resolveTypeVars, the field/variable root is only referenced after this explicit check: if (root == null) { throw new JadxRuntimeException("Null root node"); } This check occurs at the very start of the method and throws an exception immediately if root is null. Because throwing the exception aborts execution, no subsequent line (including the one with "root.getTypeUtils().replaceTypeVariablesUsingMap(...)") can be reached with root == null. There are no assignments to root inside this method, and no control-flow paths that would bypass the null-check before calling root.getTypeUtils(). Thus, for any execution path that actually reaches: ArgType resolvedType = root.getTypeUtils() .replaceTypeVariablesUsingMap(returnType, typeVarsMapping); root is guaranteed to be non-null at that point. Therefore, the expression "root" at that line cannot be null within this method’s execution. is not nullable at the point it is used. ```java root ``` Please rewrite the method so that at the line "private IMethodDetails resolveTypeVars(IMethodDetails mthDetails, Map<ArgType, ArgType> typeVarsMapping) { if (root == null) { throw new JadxRuntimeException("Null root node"); } List<ArgType> argTypes = mthDetails.getArgTypes(); int argsCount = argTypes.size(); boolean fixed = false; List<ArgType> fixedArgTypes = new ArrayList<>(argsCount); for (int argNum = 0; argNum < argsCount; argNum++) { ArgType argType = argTypes.get(argNum); if (argType == null) { throw new JadxRuntimeException("Null arg type in " + mthDetails + " at: " + argNum + " in: " + argTypes); } if (argType.containsTypeVariable()) { ArgType resolvedType = root.getTypeUtils().replaceTypeVariablesUsingMap(argType, typeVarsMapping); if (resolvedType == null || resolvedType.equals(argType)) { // type variables erased from method info by compiler resolvedType = mthDetails.getMethodInfo().getArgumentsTypes().get(argNum); } fixedArgTypes.add(resolvedType); fixed = true; } else { fixedArgTypes.add(argType); } } ArgType returnType = mthDetails.getReturnType(); if (returnType.containsTypeVariable()) { ArgType resolvedType = root.getTypeUtils().replaceTypeVariablesUsingMap(returnType, typeVarsMapping); if (resolvedType == null || resolvedType.containsTypeVariable()) { returnType = mthDetails.getMethodInfo().getReturnType(); fixed = true; } } if (!fixed) { return mthDetails; } MutableMethodDetails mutableMethodDetails = new MutableMethodDetails(mthDetails); mutableMethodDetails.setArgTypes(fixedArgTypes); mutableMethodDetails.setRetType(returnType); return mutableMethodDetails; }", at the occurrence of: " ArgType resolvedType = root.getTypeUtils().replaceTypeVariablesUsingMap(returnType, typeVarsMapping);" is replaced with `Nullability.castToNonnull(root)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>