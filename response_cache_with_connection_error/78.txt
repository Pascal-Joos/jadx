In the method below, the expression In the provided method, the only relevant code paths are: 1. The method checks `if (nameMap == null)`. 2. If `nameMap` is null, it immediately throws a `JadxRuntimeException` and execution of the method stops. The `return` statement is not reached in this path. 3. The `return nameMap.containsKey(fullName);` statement is executed only in the `else` path where the condition `nameMap == null` is false, which implies `nameMap != null`. Therefore, at the point of evaluating `nameMap.containsKey(fullName)`, `nameMap` is guaranteed to be non-null. Under standard Java semantics and control flow, the reference `nameMap` used in the return statement cannot be null. Note: This conclusion assumes normal Java execution without undefined behavior such as concurrent mutation of `nameMap` from other threads between the null check and the method call. Ignoring such race conditions (which are not evident from the snippet), the expression `nameMap` at the return line cannot be null. is not nullable at the point it is used. ```java nameMap ``` Please rewrite the method so that at the line "public boolean isClsKnown(String fullName) { if (nameMap == null) { throw new JadxRuntimeException("Classpath must be loaded first"); } return nameMap.containsKey(fullName); }", at the occurrence of: " return nameMap.containsKey(fullName);" is replaced with `Nullability.castToNonnull(nameMap)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>