In the method below, the expression At every shown call site, `getCleanSuccessors()` is used as a non‑null `List<BlockNode>`: - It is iterated in enhanced for‑loops: - `for (BlockNode node : block.getCleanSuccessors())` - `for (BlockNode s : block.getCleanSuccessors())` - Its methods are called directly: - `block.getCleanSuccessors().get(0);` - `block.getCleanSuccessors().size() == 1` - `block.getCleanSuccessors().contains(to)` - Used as a `List<BlockNode>` return from utility helpers, etc. In all these usages, a *null* value would cause immediate `NullPointerException`. The codebase consistently treats `cleanSuccessors` as a list that may be **empty** but never `null`. Additionally, where `cleanSuccessors` is recomputed, the code explicitly calls `updateCleanSuccessors()` (see `BlockExceptionHandler.wrapBlocksWithTryCatch`, which finishes with `topSplitterBlock.updateCleanSuccessors();`). This strongly implies the design contract is that `cleanSuccessors` is always initialized to a non‑null list (likely an empty list by default) and then updated, never set to `null`. There is no path in the provided code that assigns `null` to `this.cleanSuccessors`, nor any defensive null‑checks around `getCleanSuccessors()`; instead, only size/emptiness checks are present. That usage pattern is incompatible with `cleanSuccessors` being nullable. Therefore, at the specific call site `block.getCleanSuccessors()` (and generally throughout this code), the method is not expected to return `null`; if it did, much of this code would be inherently unsafe and would frequently throw at runtime, which contradicts the evident design. is not nullable at the point it is used. ```java block.getCleanSuccessors() ``` Please rewrite the method so that at the line "private void insertContinueInSwitch(BlockNode block, BlockNode out, BlockNode end) { int endId = end.getId(); for (BlockNode s : block.getCleanSuccessors()) { if (s.getDomFrontier().get(endId) && s != out) { // search predecessor of loop end on path from this successor List<BlockNode> list = BlockUtils.collectBlocksDominatedBy(mth, s, s); for (BlockNode p : end.getPredecessors()) { if (list.contains(p)) { if (p.isSynthetic()) { p.getInstructions().add(new InsnNode(InsnType.CONTINUE, 0)); } break; } } } } }", at the occurrence of: " for (BlockNode s : block.getCleanSuccessors()) {" is replaced with `Nullability.castToNonnull(block.getCleanSuccessors())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>