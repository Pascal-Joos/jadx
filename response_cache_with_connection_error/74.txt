In the method below, the expression Immediately before the line `Map<String, Set<String>> map = new HashMap<>(nameMap.size());`, the method performs an explicit null check: if (nameMap == null) { throw new JadxRuntimeException("Classpath must be loaded first"); } This conditional ensures that: 1. If `nameMap` is null, the method does not proceed; it throws an exception and control flow never reaches the `new HashMap<>(nameMap.size())` call. 2. The only way to reach the `new HashMap<>(nameMap.size())` line is through the "else" branch implied by that `if`, where `nameMap != null` is guaranteed. 3. There are no intervening assignments to `nameMap` between the null check and the `new HashMap<>(nameMap.size())` call that could reintroduce a null value. 4. Therefore, along all code paths that reach the expression `nameMap.size()`, `nameMap` is definitively non-null. Given standard Java semantics and no concurrent mutation of the field that would break the usual single-threaded assumptions within this method body, `nameMap` cannot be null at the point where `nameMap.size()` is evaluated. is not nullable at the point it is used. ```java nameMap ``` Please rewrite the method so that at the line "private void fillSuperTypesCache() { if (nameMap == null) { throw new JadxRuntimeException("Classpath must be loaded first"); } Map<String, Set<String>> map = new HashMap<>(nameMap.size()); Set<String> tmpSet = new HashSet<>(); for (Map.Entry<String, ClspClass> entry : nameMap.entrySet()) { ClspClass cls = entry.getValue(); tmpSet.clear(); addSuperTypes(cls, tmpSet); Set<String> result; if (tmpSet.isEmpty()) { result = Collections.emptySet(); } else { result = new HashSet<>(tmpSet); } map.put(cls.getName(), result); } superTypesCache = map; }", at the occurrence of: " Map<String, Set<String>> map = new HashMap<>(nameMap.size());" is replaced with `Nullability.castToNonnull(nameMap)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>