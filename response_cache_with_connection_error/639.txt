In the method below, the expression `usedInPhi` is of type `PhiInsn` (from the surrounding context where `usedInPhi.getResult()` is used, e.g. `usedInPhi.getResult().getSVar()` patterns). `PhiInsn` extends `InsnNode` and inherits `getResult()`. For phi instructions, the result is *always* present and is required by all SSA and region-processing logic. Numerous methods assume `phiInsn.getResult()` is non-null and immediately dereference it without any null checks: - `SSATransform.replacePhiWithMove`: ```java SSAVar assign = phi.getResult().getSVar(); ``` - `SSATransform.bindPhiArg`: ```java int regNum = phiInsn.getResult().getRegNum(); ``` - `PhiInsn.bindArg`: ```java RegisterArg arg = InsnArg.reg(getResult().getRegNum(), getResult().getInitType()); ``` - `PhiInsn.toString()`: ```java "PHI: " + getResult() + " = " + ... ``` - `InitCodeVariables.collectConnectedVars`: ```java SSAVar resultVar = phiInsn.getResult().getSVar(); ``` - Debug and analysis code (`DebugInfoApplyVisitor`, `DebugChecks`, `TypeInferenceVisitor.mergePhiBounds`, etc.) all do `phiInsn.getResult().getSVar()` with no null handling. There is no code path that sets a `PhiInsn` result to `null`. Generic result-stripping utilities either explicitly skip PHI or operate only on instructions whose result they can safely null out; PHI is treated as the defining assignment in SSA, so its result must exist. Because: - `usedInPhi` is a `PhiInsn`, - `PhiInsn` is constructed/configured with a non-null result and never cleared, - and the rest of the codebase relies on `getResult()` for PHI being non-null and would crash otherwise, the specific call `usedInPhi.getResult()` cannot return null. is not nullable at the point it is used. ```java usedInPhi.getResult() ``` Please rewrite the method so that at the line "private void mergePhiBounds(SSAVar ssaVar) { for (PhiInsn usedInPhi : ssaVar.getUsedInPhi()) { Set<ITypeBound> bounds = ssaVar.getTypeInfo().getBounds(); bounds.addAll(usedInPhi.getResult().getSVar().getTypeInfo().getBounds()); for (InsnArg arg : usedInPhi.getArguments()) { bounds.addAll(((RegisterArg) arg).getSVar().getTypeInfo().getBounds()); } } }", at the occurrence of: " bounds.addAll(usedInPhi.getResult().getSVar().getTypeInfo().getBounds());" is replaced with `Nullability.castToNonnull(usedInPhi.getResult())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>