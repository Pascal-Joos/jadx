In the method below, the expression At the specific call site: ```java private static boolean badMethodUsage(ClassNode cls, MethodNode useMth, AccessInfo accessFlags) { ClassNode useCls = useMth.getParentClass(); if (useCls.equals(cls)) { return false; } if (accessFlags.isSynthetic()) { // allow synthetic usage in inner class return !useCls.getParentClass().equals(cls); } return true; } ``` the invocation in question is: ```java useCls.getParentClass() ``` `useCls` is obtained from `useMth.getParentClass()` (a `MethodNode`’s parent class). For any `MethodNode` present in the decompiler’s model, its `parentClass` is expected to be a valid, non‑null `ClassNode`; otherwise almost every other use of `getParentClass()` on methods and fields throughout this codebase would risk NPEs. There are many such uses without null checks (e.g. `mth.getParentClass().getTopParentClass()`, `new JavaMethod(convertClassNode(parentCls), mthNode)`, etc.), which implicitly relies on `parentClass` never being null. The implementation of `ClassNode.getParentClass()` is: ```java public ClassNode getParentClass() { return parentClass; } ``` From the other provided usages: - `visitParentClasses`: ```java ClassNode parentCls = currentCls.getParentClass(); while (parentCls != currentCls) { ... parentCls = currentCls.getParentClass(); } ``` - `getTopParentClass`: ```java ClassNode parent = getParentClass(); return parent == this ? this : parent.getTopParentClass(); ``` - Multiple places compare `parentClass` to `this` (`parentClass != cls`, `parentClass != currentCls`) or rely on recursion, but **none** guard against `null`. This strongly implies a class invariant: for every `ClassNode` in this model, `parentClass` is never `null`; a “top” class is represented by `parentClass == this`, not by `null`. If `parentClass` could be `null` for any `ClassNode` that can appear as `useCls`, then code like: ```java ClassNode parent = getParentClass(); return parent == this ? this : parent.getTopParentClass(); ``` would throw an NPE on `parent.getTopParentClass()`, and code like: ```java while (parentCls != currentCls) { ... parentCls = currentCls.getParentClass(); } ``` would dereference `null` as well. The extensive, unchecked usage of `getParentClass()` throughout confirms that the design assumption is “never null”. Therefore, for this particular call site, `useCls` is always a `ClassNode` with a non‑null `parentClass` field (possibly equal to itself), so: ```java useCls.getParentClass() ``` cannot evaluate to `null. is not nullable at the point it is used. ```java useCls.getParentClass() ``` Please rewrite the method so that at the line "private static boolean badMethodUsage(ClassNode cls, MethodNode useMth, AccessInfo accessFlags) { ClassNode useCls = useMth.getParentClass(); if (useCls.equals(cls)) { return false; } if (accessFlags.isSynthetic()) { // allow synthetic usage in inner class return !useCls.getParentClass().equals(cls); } return true; }", at the occurrence of: " return !useCls.getParentClass().equals(cls);" is replaced with `Nullability.castToNonnull(useCls.getParentClass())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>