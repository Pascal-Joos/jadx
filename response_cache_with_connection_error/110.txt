In the method below, the expression Within the shown method, `mapping.setClasses(new ArrayList<>(classes.size()));` is invoked unconditionally before the `for` loop where `mapping.getClasses().add(jsonCls);` is called. Assuming: - `mapping` itself is non-null when `fillMapping` is called, and - `setClasses(...)` correctly stores the provided list and `getClasses()` simply returns what was set, then: 1. `new ArrayList<>(classes.size())` is never null. 2. `setClasses(...)` is always called before any use of `getClasses()` in this method. 3. There is no intervening code that could reset the classes list to null between `setClasses(...)` and `getClasses()`. Under these normal Java semantics and in the absence of any unusual behavior (e.g., a `setClasses` implementation that ignores its argument or intentionally stores `null`), `mapping.getClasses()` cannot be null at the point of `mapping.getClasses().add(jsonCls);`. Therefore, based solely on this method and standard expectations for getters/setters, `mapping.getClasses()` cannot be null here. is not nullable at the point it is used. ```java mapping.getClasses() ``` Please rewrite the method so that at the line "private static void fillMapping(JsonMapping mapping, RootNode root) { List<ClassNode> classes = root.getClasses(true); mapping.setClasses(new ArrayList<>(classes.size())); for (ClassNode cls : classes) { ClassInfo classInfo = cls.getClassInfo(); JsonClsMapping jsonCls = new JsonClsMapping(); jsonCls.setName(classInfo.getRawName()); jsonCls.setAlias(classInfo.getAliasFullName()); jsonCls.setInner(classInfo.isInner()); jsonCls.setJson(cls.getTopParentClass().getClassInfo().getAliasFullPath() + ".json"); if (classInfo.isInner()) { jsonCls.setTopClass(cls.getTopParentClass().getClassInfo().getFullName()); } addFields(cls, jsonCls); addMethods(cls, jsonCls); mapping.getClasses().add(jsonCls); } }", at the occurrence of: " mapping.getClasses().add(jsonCls);" is replaced with `Nullability.castToNonnull(mapping.getClasses())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>