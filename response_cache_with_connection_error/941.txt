In the method below, the expression At every shown call site, `varInfo` (or `var`) is a `TypeSearchVarInfo` instance obtained from `state.getVarInfo(...)` or from `state.getUnresolvedVars()` / `vars`. For each such variable, `collectConstraints` is called in `run()`: ```java private void collectConstraints(SSAVar var) { TypeSearchVarInfo varInfo = state.getVarInfo(var); if (varInfo.isTypeResolved()) { varInfo.setConstraints(Collections.emptyList()); return; } varInfo.setConstraints(new ArrayList<>()); addConstraint(varInfo, makeConstraint(var.getAssign())); for (RegisterArg regArg : var.getUseList()) { addConstraint(varInfo, makeConstraint(regArg)); } } ``` Both branches of `collectConstraints` assign a **non-null** list to `varInfo` via `setConstraints`: - `Collections.emptyList()` (never null) - `new ArrayList<>()` (never null) After this, all uses of `varInfo.getConstraints()` in the provided code (`singleCheck`, `addConstraint`, `resolveIndependentVariables`, `fullCheck`) operate on this initialized `constraints` field. The `addConstraint` method only calls `varInfo.getConstraints().add(...)`, which assumes and uses this non-null list; there is no code shown that ever assigns `null` to `constraints`. Thus, at the provided call site `varInfo.getConstraints()`, the method returns the `constraints` field, which has been initialized to a non-null list and is never set to null in the shown code. Therefore it cannot be null at this call site. is not nullable at the point it is used. ```java varInfo.getConstraints() ``` Please rewrite the method so that at the line "private void addConstraint(TypeSearchVarInfo varInfo, ITypeConstraint constraint) { if (constraint != null) { varInfo.getConstraints().add(constraint); } }", at the occurrence of: " varInfo.getConstraints().add(constraint);" is replaced with `Nullability.castToNonnull(varInfo.getConstraints())` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>