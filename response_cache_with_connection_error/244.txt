In the method below, the expression At the call site ```java InsnNode callInsn = customNode.getCallInsn(); ``` `customNode` is an instance of `InvokeCustomNode` created specifically by `CustomLambdaCall.buildLambdaMethodCall`, which in turn calls `buildMethodCall`. Inside `buildMethodCall`: ```java InvokeCustomNode invokeCustomNode = new InvokeCustomNode(lambdaInfo, insn, false, isRange); // ... InvokeNode invokeNode = buildInvokeNode(methodHandleType, invokeCustomNode, callMthInfo); if (methodHandleType == MethodHandleType.INVOKE_CONSTRUCTOR) { ConstructorInsn ctrInsn = new ConstructorInsn(mth, invokeNode); invokeCustomNode.setCallInsn(ctrInsn); } else { invokeCustomNode.setCallInsn(invokeNode); } ``` In all non-exceptional code paths that return `invokeCustomNode`, `setCallInsn` is called with either: - a newly constructed `ConstructorInsn`, or - a newly constructed `InvokeNode`. Both are non-null local variables just created with `new`. No alternative branch exists where `setCallInsn` is skipped or passed `null`. After this point `invokeCustomNode` is returned and becomes the `customNode` used by `InsnGen.makeInvokeLambda`. In `makeSimpleLambda` and `makeRefLambda`, the same `getCallInsn()` getter is used: ```java InsnNode callInsn = customNode.getCallInsn(); ``` Those methods are only entered after the `InvokeCustomNode` has been fully constructed as shown above; there is no further mutation that can reset `callInsn` to null. Therefore, at this specific call site, `callInsn` has been set to a non-null `InsnNode` earlier in `buildMethodCall`. Since the field is always initialized along the only normal-return path that produces this `customNode`, `getCallInsn()` cannot return null at this call site. is not nullable at the point it is used. ```java callInsn ``` Please rewrite the method so that at the line "private void makeSimpleLambda(ICodeWriter code, InvokeCustomNode customNode) { try { InsnNode callInsn = customNode.getCallInsn(); MethodInfo implMthInfo = customNode.getImplMthInfo(); if (implMthInfo == null) { throw new JadxRuntimeException("Failed to generate simple lambda: implMthInfo is null"); } int implArgsCount = implMthInfo.getArgsCount(); if (implArgsCount == 0) { code.add("()"); } else { code.add('('); int callArgsCount = callInsn.getArgsCount(); int startArg = callArgsCount - implArgsCount; if (customNode.getHandleType() != MethodHandleType.INVOKE_STATIC && customNode.getArgsCount() > 0 && customNode.getArg(0).isThis()) { callInsn.getArg(0).add(AFlag.THIS); } if (startArg >= 0) { for (int i = startArg; i < callArgsCount; i++) { if (i != startArg) { code.add(", "); } addArg(code, callInsn.getArg(i)); } } else { code.add("/* ERROR: " + startArg + " */"); } code.add(')'); } code.add(" -> {"); if (fallback) { code.add(" // ").add(implMthInfo.toString()); } code.incIndent(); code.startLine(); if (!implMthInfo.getReturnType().isVoid()) { code.add("return "); } makeInsn(callInsn, code, Flags.INLINE); code.add(";"); code.decIndent(); code.startLine('}'); } catch (Exception e) { throw new JadxRuntimeException("Failed to generate 'invoke-custom' instruction: " + e.getMessage(), e); } }", at the occurrence of: " int callArgsCount = callInsn.getArgsCount();" is replaced with `Nullability.castToNonnull(callInsn)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>