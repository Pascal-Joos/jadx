In the method below, the expression Given the exact method implementation: public int getClassesCount() { return classes != null ? classes.length : 0; } the expression at the line "return classes.length;" is evaluated only when the ternary condition `classes != null` is true. Control flow: - If `classes` is null, the condition `classes != null` is false, and the method returns `0`. The expression `classes.length` is not evaluated. - If `classes` is not null, the condition `classes != null` is true, and then `classes.length` is evaluated and returned. Due to Java’s short-circuit evaluation of the ternary operator, `classes.length` is never evaluated when `classes` is null. Therefore, in this method as written, the `classes` reference at the point of `classes.length` cannot be null. is not nullable at the point it is used. ```java classes ``` Please rewrite the method so that at the line "public int getClassesCount() { return classes != null ? classes.length : 0; }", at the occurrence of: " return classes.length;" is replaced with `Nullability.castToNonnull(classes)` See the example below, for expression `foo.getBar()`, wrap it with `Nullability.castToNonnull(foo.getBar())`. ```java public t(){ String l = foo.getBar(); } ``` will be: ```java public t(){ String l = Nullability.castToNonnull(foo.getBar()); } ``` ### Instructions: Do not import any additional classes. Do not add Java comments, but also do not remove any preexisting comments. Only modify relevant lines as required. Do not address any other issues. Return only the modified method wrapped in the XML format shown below. Place the Java code inside the <code> tag using a CDATA block with ```java formatting. Just give me the fixed code snippet in XML format in <response> tag like the example below. I will apply the fix to my codebase. Do not import any additional libraries or classes or add java comments. If you can provide the fixed code snippet, please provide it in the following format and place the code snippet in the <code> tag within ```java block. <response> <success>true</success> <code><![CDATA[ ```java Your fixed code snippet here ``` ]]> </code> </response>