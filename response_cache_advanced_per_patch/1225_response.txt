<response>
  <success>true</success>
  <code><![CDATA[
  ```java
private void parseNameSpace() throws IOException {
 		if (is == null) {
 			die("ParserStream is not initialized");
 		}
 		int headerSize = Nullability.castToNonnull(is).readInt16();
 		if (headerSize > 0x10) {
 			LOG.warn("Invalid namespace header");
 		} else if (headerSize < 0x10) {
 			die("NAMESPACE header is not 0x10 big");
 		}
 		int size = Nullability.castToNonnull(is).readInt32();
 		if (size > 0x18) {
 			LOG.warn("Invalid namespace size");
 		} else if (size < 0x18) {
 			die("NAMESPACE header chunk is not 0x18 big");
 		}
 
 		int beginLineNumber = Nullability.castToNonnull(is).readInt32();
 		int comment = Nullability.castToNonnull(is).readInt32();
 		int beginPrefix = Nullability.castToNonnull(is).readInt32();
 		int beginURI = Nullability.castToNonnull(is).readInt32();
 		Nullability.castToNonnull(is).skip(headerSize - 0x10);
 
 		String nsKey = getString(beginURI);
 		String nsValue = getString(beginPrefix);
 		if (StringUtils.notBlank(nsKey) && !nsMap.containsValue(nsValue)) {
 			nsMap.putIfAbsent(nsKey, nsValue);
 		}
 		namespaceDepth++;
}
  ```
    ]]>
  </code>
</response>